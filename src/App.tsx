// Imports the main WeatherNowApp component that contains all app logic and UI
import WeatherNowApp from "./WeatherNowApp";

// Root component that simply renders the WeatherNowApp
export default function App() {
  return <WeatherNowApp />;
}
